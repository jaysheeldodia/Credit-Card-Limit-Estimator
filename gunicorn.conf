workers = 4  # Adjust the number of workers based on your needs
bind = "0.0.0.0:5000"  # Bind to all interfaces on port 5000
accesslog = "/path/to/access.log"  # Set the access log path
errorlog = "/path/to/error.log"  # Set the error log path